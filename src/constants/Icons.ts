import {
  PlusCircle,
  MinusCircle,
  SlidersHorizontal,
  ShoppingCart,
  XSquare,
  Trash2,
  Loader2,
} from 'lucide-react';

const Icons = {
  plus: PlusCircle,
  minus: MinusCircle,
  filter: SlidersHorizontal,
  cart: ShoppingCart,
  close: XSquare,
  delete: Trash2,
  loader: Loader2,
};

export default Icons;
